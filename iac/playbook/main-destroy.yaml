- name: delete machine gcp_instances
  hosts: localhost
  vars:
    ansible_python_interpreter: /opt/gcp-venv/bin/python3
  become: true
  vars_files:
    - ../vars/machine.yaml
  tasks:
    - import_tasks: ./tasks/destroy-machine-tasks.yaml