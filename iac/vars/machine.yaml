# contains all configuration related to the machines that we might use for the whole infrastructure setup 
# service_account_path_server: "/home/window/credentials/"
# service_account_path_local: "/home/seang/devops-itp-gen1/ansible-related/lac/task2/credentials/serviceaccount.json"
adc_file: "/home/seang/.config/gcloud/application_default_credentials.json"
google_project_id: "project-9445469d-6690-4a81-aef"
ssh_pubkey_path: "/home/seang/.ssh/id_rsa.pub"
agent_user: "seang"

# Define machine spec
machine_specs:
  master01:
    machine_type: "e2-standard-2"
    zone: "asia-southeast2-a"
    disk_size: 50
    disk_type: "pd-standard"
  master02:
    machine_type: "e2-standard-2"   # was e2-worker01
    zone: "asia-east2-a"
    disk_size: 50
    disk_type: "pd-standard"
  master03:
    machine_type: "e2-standard-2"   # was e2-worker01
    zone: "asia-east1-a"
    disk_size: 50
    disk_type: "pd-standard"
  worker01:
    machine_type: "e2-standard-2"   # was e2-worker01
    zone: "australia-southeast2-c"
    disk_size: 50
    disk_type: "pd-standard"
  worker02:
    machine_type: "e2-standard-2"   # was e2-worker01
    zone: "asia-northeast2-a"
    disk_size: 50
    disk_type: "pd-standard"
  worker03:
    machine_type: "e2-standard-2"   # was e2-worker01
    zone: "asia-northeast3-b"
    disk_size: 50
    disk_type: "pd-standard"
  worker04:
    machine_type: "e2-standard-2"
    zone: "asia-east1-a"
    disk_size: 50
    disk_type: "pd-standard"
  worker05:
    machine_type: "e2-standard-2"
    zone: "australia-southeast2-a"
    disk_size: 50
    disk_type: "pd-standard"

ubuntu_specs:
  v22:
    image: "projects/ubuntu-os-cloud/global/images/family/ubuntu-2204-lts"
  v24:
    image: "projects/ubuntu-os-cloud/global/images/family/ubuntu-2404-lts"

# Define machines that we will create
machines_info:
  # master requirement : RAM 8G, ....
  - name: "master01"
    machine_type: "{{ machine_specs.master01.machine_type }}"
    image: "{{ ubuntu_specs.v22.image }}"
    zone: "{{ machine_specs.master01.zone }}"
    disk_size: "{{ machine_specs.master01.disk_size }}"
    disk_type: "{{ machine_specs.master01.disk_type }}"
  - name: "master02"
    machine_type: "{{ machine_specs.master02.machine_type }}"
    image: "{{ ubuntu_specs.v22.image }}"
    zone: "{{ machine_specs.master02.zone }}"
    disk_size: "{{ machine_specs.master02.disk_size }}"
    disk_type: "{{ machine_specs.master02.disk_type }}"
  - name: "master03"
    machine_type: "{{ machine_specs.master02.machine_type }}"
    image: "{{ ubuntu_specs.v22.image }}"
    zone: "{{ machine_specs.master03.zone }}"
    disk_size: "{{ machine_specs.master03.disk_size }}"
    disk_type: "{{ machine_specs.master03.disk_type }}"
  - name: "worker01"
    machine_type: "{{ machine_specs.worker01.machine_type }}"
    image: "{{ ubuntu_specs.v22.image }}"
    zone: "{{machine_specs.worker01.zone}}"
    disk_size: "{{ machine_specs.worker01.disk_size }}"
    disk_type: "{{ machine_specs.worker01.disk_type }}"
  - name: "worker02"
    image: "{{ ubuntu_specs.v22.image }}"
    machine_type: "{{ machine_specs.worker02.machine_type }}"
    zone: "{{ machine_specs.worker02.zone }}"
    disk_size: "{{ machine_specs.worker02.disk_size }}"
    disk_type: "{{ machine_specs.worker02.disk_type }}"
  - name: "worker03"
    image: "{{ ubuntu_specs.v22.image }}"
    machine_type: "{{ machine_specs.worker03.machine_type }}"
    zone: "{{ machine_specs.worker03.zone }}"
    disk_size: "{{ machine_specs.worker03.disk_size }}"
    disk_type: "{{ machine_specs.worker03.disk_type }}"
  # - name: "worker04"
  #   image: "{{ ubuntu_specs.v22.image }}"
  #   machine_type: "{{ machine_specs.worker04.machine_type }}"
  #   zone: "{{ machine_specs.worker04.zone }}"
  #   disk_size: "{{ machine_specs.worker04.disk_size }}"
  #   disk_type: "{{ machine_specs.worker04.disk_type }}"
  # - name: "worker05"
  #   image: "{{ ubuntu_specs.v22.image }}"
  #   machine_type: "{{ machine_specs.worker05.machine_type }}"
  #   zone: "{{ machine_specs.worker05.zone }}"
  #   disk_size: "{{ machine_specs.worker05.disk_size }}"
  #   disk_type: "{{ machine_specs.worker05.disk_type }}"
